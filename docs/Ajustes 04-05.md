Primero valida que lo cambios que desarrollemos local se puedan ver reflejados automáticamente en docker local que sale por el puerto 8502

-El requerimiento principal es habilitar la edición completa de un formulario cuando tiene un estado borrador, por ejemplo para este caso:
![[Pasted image 20260504182403.png]]

Segundo requerimiento es automatizar el actions runner de github, para cuando se realice push al repo para desplegar en el server ol9, los ajuste se desplieguen automáticamente. 
*Ayúdame con el paso a paso detallado.