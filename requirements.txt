# Runtime dependencies for the Streamlit app.
# This file is intentionally minimal so Linux deployments only install
# what the project actually uses.
altair==6.0.0
pandas==2.3.3
pyodbc==5.3.0
SQLAlchemy==2.0.48
streamlit==1.55.0
